// ignore_for_file: constant_identifier_names

enum AbsNormalStatus { INITIAL, LOADING, ERROR, SUCCESS }

enum PermissionFor { camera, gallery, storage, contact, microphone, location }

enum ToastType { SUCCESS, ERROR, WARNING, INFO }
